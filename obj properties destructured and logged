//Original:

const person = {
    name: "Noam Chomsky",
    age: 92
};

let name = person.name;
let age = person.age;
console.log(name);
console.log(age);


//Refactored:

const person = {
    name: "Noam Chomsky",
    age: 92
};

const { name, age } = person;
console.log(name);
console.log(age);


//Explanation: Object destructuring allows for unpacking properties directly from the object. This reduces redundancy and makes the code more readable.
